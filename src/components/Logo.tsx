import * as React from "react";

export interface ILogoProps extends React.SVGAttributes<HTMLOrSVGElement> {}

const Logo: React.FunctionComponent<ILogoProps> = (props: ILogoProps) => {
  return <div></div>;
};

export default Logo;
